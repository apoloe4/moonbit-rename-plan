name = "apoloe4/rename_plan"

version = "0.1.0"

readme = "README.md"

repository = "https://github.com/apoloe4/moonbit-rename-plan"

license = "MIT"

keywords = [ "rename", "planning", "recovery", "filesystem" ]

description = "Deterministic batch rename planning, prefix simulation and snapshot recovery analysis"

preferred_target = "wasm-gc"

supported_targets = "+js+wasm-gc"
